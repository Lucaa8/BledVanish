name: ${name}
version: ${project.version}
description: ${description}
author: Luca008
main: ${mainClass}
api-version: 1.20
prefix: BledVanish
depend:
  - OpenInv
commands:
  vanish:
    description: Toggle a player's vanish state
    aliases:
      - v
    permission: bledvanish.command.vanish
    permission-message: §cYou do not have permission!
permissions:
  bledvanish.command.vanish:
    description: Allows a player to execute the vanish command and see other vanished players
    default: op